namespace DevTaskManager.Domain.Enums;

public enum UserRole { Admin, Developer }

public enum SeniorityLevel { Junior, Mid, Senior }

public enum AvailabilityStatus { Available, Busy, Vacation }

public enum ProjectStatus { Planning, InProgress, OnHold, Completed, Cancelled }

public enum TaskStatus { Todo, InProgress, Review, Done, Blocked }

public enum TaskPriority { Low, Medium, High, Critical }
